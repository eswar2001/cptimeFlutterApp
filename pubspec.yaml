name: cptime
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  intl: ^0.17.0
  url_launcher: ^6.0.2
  google_fonts: ^2.0.0
  cupertino_icons: ^1.0.2
  splashscreen: ^1.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - resources/day19-apple-watch.png
